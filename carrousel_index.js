document.addEventListener('DOMContentLoaded', function () {
  var slider = tns({
    container: '#center',
    items: 2,
    center: true,
    mouseDrag: true,
    swipeAngle: false,
    nav: false,
    controls: false,
  });
});
